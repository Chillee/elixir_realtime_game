{
    "version": "0.2.0",
    "configurations": [

        {
            "runtimeExecutable": "chromium-browser",
            "type": "chrome",
            "request": "launch",
            "name": "Attach to Chrome",
            "url": "http://localhost:4000",
            "webRoot": "${workspaceRoot}"
        }
    ]
}